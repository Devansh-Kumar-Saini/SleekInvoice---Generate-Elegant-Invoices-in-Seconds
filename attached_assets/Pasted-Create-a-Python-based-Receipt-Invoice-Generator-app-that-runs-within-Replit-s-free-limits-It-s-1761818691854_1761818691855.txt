Create a Python-based Receipt (Invoice) Generator app that runs within Replit’s free limits.

It should:

🧩 Core Features
Ask user input for:

Company Name

Company Logo (upload or image URL)

Date (auto-detect current date but allow manual edit)

Customer Name

Customer Info (email, phone, address)

Product Category (e.g., electronics, groceries, etc.)

Items list: each with:

Item name Quantity Price per unit (optional) Additional details (e.g., warranty, SKU, etc.)
Automatically calculate:

Subtotal

Tax (optional: ask % or default 10%)

Grand Total

💡 Extra Features
Option to add discount (flat or percentage)

Option to save receipts as:

Printable HTML/PDF file (simple, no heavy libraries — can use reportlab or fpdf)

Or just display formatted in terminal/text output

Auto-generate a unique invoice number

Display company logo (if image URL, embed in HTML/PDF)

Automatically store previous receipts in a receipts/ folder

Option to export as .csv (items + totals)

Option to choose currency (default USD)

Validate numeric inputs

🧠 Technical Details
Must run in a single Python file (main.py) Should work completely in console (text-based) or optionally generate a simple HTML output (if easy) Avoid heavy dependencies (e.g., no Flask, Django) Allowed libraries: datetime, os, json, fpdf, csv Include clear comments and instructions Keep code clean, readable, and beginner-friendly Use functions for modularity (add_items(), generate_pdf(), etc.)
✨ Bonus (if possible within limits)
Support multiple categories per invoice Preview receipt before saving Allow editing items before finalizing Auto-format prices (e.g., $12.99)
🖼️ Output Example (in console)
--------------------------------------------------
Company: AlphaTech Solutions
Date: 2025-10-30
Invoice #: INV-20251030-001

Customer: John Doe
Category: Electronics

Items:
1. Laptop (x1) ............ $1200
2. Mouse (x2) ............. $50
Subtotal: $1250
Tax (10%): $125
Total: $1375

Notes: Includes 1-year warranty.
--------------------------------------------------
Receipt saved as receipts/INV-20251030-001.pdf
Generate the full working code and include short usage instructions.

