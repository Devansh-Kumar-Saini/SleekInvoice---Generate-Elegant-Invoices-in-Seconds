{
  "buildCommand": "cd Product && npm install && npm run build",
  "outputDirectory": "Product/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
